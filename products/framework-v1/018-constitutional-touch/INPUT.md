---
atom: ATOM-018
product: Constitutional touch v1 (amendments to the five framework documents + human profile)
parent: Recursive Product Framework v1
formulated_by: Human (accumulated gate reservations) + formulating agent (launch session), 2026-07-07; chain order per GATE-012
verification: independent + human
maturity_target: reviewed
instantiation: session
model_tier: L — constitutional normative writing (MT3)
budget: ~205k tokens (reads ~47k ×3.5 + 40k — the GATE-011 executor-envelope lesson applied)
recursion_allowance: 1
---

# ATOM-018 — Input Specification

## Perspective map — ATOM-018 (PM1–PM4)

**Question under examination:** do the amendments integrate the accumulated queue without relaxing anything?
**Fan triggers (PM6):** decomposition plan = no; maturity ≥ reviewed = yes; crosses perimeter = no.
**Fan decision:** opt-out per PM4.

Opt-out: single lens — narrow question — the queue consists of already-decided
items (gate reservations, recorded findings, accepted decision records); the work
is constitutional integration under one professional optic (framework-architect);
correctness risk is carried by blind verification + the mandatory human gate, not
by a fan.

## 1. Product Identity
| Field | Value |
| :---- | :---- |
| Product | Amendments in place: `framework/ATOM-SPEC.md`, `framework/FEV-PROTOCOL.md`, `framework/REPO-STRUCTURE.md`, `framework/ORCHESTRATION.md`, `framework/SYNTHESIS-PROTOCOL.md`, `roles/humans/ghenadie.md` |
| Atom ID | ATOM-018 |
| Parent product | Recursive Product Framework v1 |
| Executor role | framework-architect (exists) |
| Input product(s) | The six files above (current state); queue sources: `products/business-strategy-v1/006-business-strategy/RESULT.md` (§Framework findings 1–5); `decisions/GATE-010-…` (HP2-extension reservation, verbatim); `decisions/GATE-011-…` (S5 plain-language episode + envelope lesson); `decisions/INFO-005-…` (full D7); `knowledge/precedent/verify-envelope-calibration.md` |
| Consumer(s) | Every future atom (the constitution); ATOM-007 (executes immediately after under the amended rules) |
| Maturity target | reviewed |
| Gates | GATE-014 — human acceptance after Verify (constitutional change — mandatory per GATE-012). Brief in Russian, plain language, **applying amendment T1 to itself**: the brief carries a Russian summary of every amendment |
| Instantiation | session |
| Verification mode | independent + human |
| Budget envelope | ~205k tokens |
| Recursion-depth allowance | 1 |

## 2. Job To Be Done
Integrate every queued constitutional obligation accumulated by closed atoms into
the five framework documents and the human profile — so that no rule any gate or
finding has already made binding lives only in a decision record. After this
touch, ATOM-007 runs under a constitution with zero known deferred amendments.

## 3. Context — The Queue (accepted decisions and reservations — formalize, do not reopen)

- T1 — **HP2 extension + plain-language briefs** (GATE-010 reservation, verbatim: «gate-пакеты с судейством документов включают русскую справочную копию (расширение HP2, в FEV-touch)»; reinforced by the GATE-011 S5 episode — the human could not parse a criterion-form question until restated plainly). Amend FEV-PROTOCOL (gate-brief/VP18 area): a gate package asking the human to judge a document MUST include a reference copy or faithful summary of the judged document in the human's `preferred_for_decisions` language, AND every human-judged criterion MUST be restated in plain language in the brief. Update `roles/humans/ghenadie.md` *Decision-input preferences* accordingly.
- T2 — **HP4 migration** (REPO-STRUCTURE HP4, promised): HP2–HP3 move to FEV-PROTOCOL, their normative home; REPO-STRUCTURE §4 keeps the profile mechanics and gains a pointer.
- T3 — **F3 row-2 precedence** (ATOM-006 finding 1, hit ×2): explicit wording for the precedence between «feeds ≥ 2 downstream atoms» and INPUT-granted per-risk verification discretion. Target: ATOM-SPEC F3 and/or FEV-PROTOCOL FP8 — one home, the other references.
- T4 — **Inline-conflation legality** (ATOM-006 finding 2): ORCHESTRATION states when formulator-executes-inline (subagent semantics) is legal by rule, instead of per-INPUT sanctioning.
- T5 — **Slim-verify codification** (ATOM-006 finding 3; GATE-008 precedent): FEV-PROTOCOL legalizes the slim blind package under exactly two conditions — (a) all hard criteria script-checkable; (b) the package lists every file the product cites — plus the ≥25% envelope margin note from the calibration precedent.
- T6 — **Envelope discipline** (ATOM-006 finding 5 + GATE-011 executor lesson): ORCHESTRATION EC — writing-heavy executor envelopes are sized ×3.5 + 40k like verifiers; the EC1 2× floor is a minimum, not a calibration.
- T7 — **SYNTHESIS-PROTOCOL migrations land** (SP5, SY1, GB5): E8 joins the ATOM-SPEC §5.7 trigger table; `ATOM-NNN-SYNTH` joins REPO-STRUCTURE NC1; `SYNTHESIS.md` and `GATE-BRIEF-<gate-id>.md` join NC7 reserved filenames.
- T8 — **Full D7** (INFO-005, verbatim in the record): ATOM-SPEC — O4.2 extended transitively; the §6.2 RESULT template frontmatter gains `total_descendants`, `max_depth_reached`, `subtree_cost` with mandatory execute/verify/role-creation/synthesis breakdown, for every parent atom; SYNTHESIS-PROTOCOL LF7 aligned to the transitive scheme. Purpose text (cost-of-insight metric, Formulate calibration, client cost line) belongs in one *(informative)* passage, not in rules.
- T9 — **Perspective map lands in the plan template** (SYNTHESIS-PROTOCOL PM2, promised at ORCHESTRATION's next touch): the map section joins OD2 contents and the §8.2 template.
- T10 — **Amendment provenance**: this is the constitution's first touch — define and apply a minimal provenance mechanism (e.g. an «Amended» row in each touched document's header table naming atom + date; rules changed cite nothing retroactively). Executor decides the mechanism (O9.1), applies it uniformly, and states it in RESULT.

## 4. Deliverable
The six files of §1, amended in place. No new files. No renumbering of existing
rules; new rules take fresh numbers in their document's existing prefix spaces.

## 5. Definition of Done
**Hard criteria:**
- H1. A coverage table in `RESULT.md` maps each of T1–T10 to the exact rule ids /
  sections amended (RC4 discipline); every row non-empty. Check: table present.
- H2. E8 row present in ATOM-SPEC §5.7 table; `-SYNTH` in REPO-STRUCTURE NC1;
  `SYNTHESIS.md` and `GATE-BRIEF` in NC7. Check: grep.
- H3. ATOM-SPEC §6.2 template frontmatter contains `total_descendants`,
  `max_depth_reached`, `subtree_cost` and the four-type breakdown. Check: grep.
- H4. HP2–HP3 text present in FEV-PROTOCOL; REPO-STRUCTURE §4 carries the
  migration pointer; ghenadie.md updated. Check: grep.
- H5. ORCHESTRATION §8.2 template contains the perspective-map section. Check: grep.
- H6. Every touched document carries the T10 provenance mark. Check: grep.
- H7. No rule-prefix collisions introduced; each document's declared fenced-block
  count still matches its actual count (T2/TP2/SY12/LP7 discipline). Check: script.
- H8. Combined length growth of the five framework documents ≤ 25% (amendments,
  not rewrites). Check: wc -l vs pre-touch counts recorded in run.log.
**Soft criteria:**
- S1. Tightens-never-relaxes holds for every amendment (R0.3) — judge: Verify agent.
- S2. Amendments are minimal — no drive-by rewording outside the T-queue — judge:
  Verify agent (no-drift discipline).
- S3. The T3 precedence wording decides both recorded ATOM-006 ambiguity cases
  unambiguously — judge: Verify agent.
- S4. The five documents still read as one system: every cross-reference between
  them resolves after the amendments — judge: Verify agent.
- S5. Human acceptance of the amended constitution — judge: human (GATE-014).

**Maturity target:** reviewed. Do not gold-plate.

## 6. Method Hints (non-binding)
- Work document by document; keep a pre-touch `wc -l` snapshot in the run log
  first (H8 baseline).
- T1/T2 land together (HP2's new home receives its extension in the same motion).
- For T8, the O4.2 transitive extension is the rule; the template fields are its
  operational face — keep them consistent with the INFO-005 verbatim.
- The forward-reference sentences being fulfilled (SP5, SY1, GB5, HP4, PM2)
  SHOULD be updated to past-tense pointers («landed per ATOM-018») rather than
  deleted — they are part of the historical reading of those documents.
- Do not touch MANIFEST/README (ATOM-017 just closed them; naming F1–F3 minors of
  ATOM-005 are NOT in this queue — they belong to a public-face touch, not the
  constitution).

## 7. Escalation
Stop and escalate (one consolidated question set) on triggers E1–E8. If any two
queue items contradict each other when formalized — that is E7 to the formulating
agent, not a silent resolution. Gates placed: GATE-014 after Verify. Otherwise:
do not ask for permission on the normal path. Execute.
