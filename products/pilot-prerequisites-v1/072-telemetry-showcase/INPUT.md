---
atom: ATOM-072
product: Telemetry + consent RO/RU + work showcase (deliverables §4.3 + §4.5 of ATOM-007 INPUT)
parent: Pilot Prerequisites v1 (ATOM-007; plan: 007-pilot-prerequisites/PLAN.md)
formulated_by: formulating agent (launch session), 2026-07-07
verification: independent (slim VP20) — feeds 4 lens atoms + perimeter (consent, telemetry)
maturity_target: reviewed
instantiation: subagent
model_tier: M
budget: ~120k tokens (raised per GATE-016 subagent-overhead calibration)
recursion_allowance: 0
---

# ATOM-072 — Input Specification

## 1. Product Identity
| Field | Value |
| :---- | :---- |
| Product | `products/pilot-prerequisites-v1/072-telemetry-showcase/` — `telemetry/` + `consent/` + `showcase/` per §4 |
| Atom ID | ATOM-072 |
| Executor role | pilot-toolsmith (`roles/pilot-toolsmith.md` — read it first, it is your identity) |
| Input product(s) | This INPUT; `products/business-strategy-v1/pilot/pilot-design.md` (**consent master text EN + closed telemetry list — the law of this atom**); `decisions/INFO-005-d7-subtree-accounting-detail.md` (cost-line fields, verbatim); `decisions/INFO-006-atom-007-intake-decisions.md` (P3 showcase, P5 telemetry); `decisions/INFO-007-field-test-lesson-2-team-visibility.md` (TEAM summary, verbatim); `framework/ATOM-SPEC.md` §6.2 + O4.3 only (the RESULT fields the renders read); `products/framework-v1/status.yaml` + `products/framework-v1/018-constitutional-touch/RESULT.md` + `decisions/GATE-011-atom-016-acceptance.md` (real data for worked examples) |
| Consumer(s) | 4 lens atoms; pilot founders + Startup Moldova (consent, cost line — perimeter); the pilot operator; pricing re-anchor |
| Verification mode | independent (slim VP20) |
| Budget envelope | ~120k tokens (GATE-016) |

## 2. Job To Be Done
The founder can consent honestly (reading every promise in their language and
every line of the script those promises bind), the telemetry cannot leak
anything outside the closed list, and the work becomes **visible**: every
founder-facing reply carries the cost line, every parent-atom closure renders a
TEAM summary — the invisible team was field-test lesson №2.

## 3. Binding constraints (accepted decisions — do not reopen)
- Telemetry scope: **closed whitelist** from pilot-design (STATUS.md, RESULT.md
  cost blocks, run.log, status.yaml, verify verdicts). No business content —
  no INPUT bodies, no deliverables, no decision texts, no code. Deny-by-default.
- Push target: `qroky/pilot-telemetry`. Script **open** — founder-readable,
  commented. Deactivatable by the founder; deletion on a **fixed date** (fill-in
  field at signing, pilot end + 30 days default), or earlier on request.
- Consent master = pilot-design.md text (EN). Render RO + RU **substance-
  identical** — the 4 red lines (INFO-001), deletion clause, open-script clause
  present in both. Do NOT re-draft the master's substance.
- Cost line format (INFO-006 P3, fixed): **«⚙ N агентов · M ролей · глубина D ·
  возвратов verify R · $X»** — every element derived from named INFO-005/O4.3
  fields (total_descendants → N; distinct roles in subtree → M;
  max_depth_reached → D; verify returns_used sum → R; subtree_cost → $X).
- TEAM summary (INFO-007, verbatim in the record): rendered at each parent-atom
  closure from existing records only (status.yaml / verdicts / RESULT subtree
  fields / run.log) — sections: roles + one-line contribution each; the lens
  map; synthesis conflicts with outcomes («линза X хотела A, линза Y забраковала
  из-за B, синтез выбрал C»); verify returns. Plain language, founder-readable.

## 4. Deliverable
1. `telemetry/push.sh` — whitelist-enforcing push to `qroky/pilot-telemetry`;
   explicit whitelist array at the top; deny-by-default copy logic; founder-off
   switch (`telemetry/OFF` file → script exits 0 doing nothing, loudly saying
   so); heavily commented for a lay reader.
2. `telemetry/negative-test.sh` + `telemetry/negative-test-output.txt` — proves
   a non-whitelisted file (e.g. a planted `INPUT.md` and a `secret.txt`) is NOT
   staged/pushed; output recorded.
3. `consent/CONSENT.ro.md` + `consent/CONSENT.ru.md` — renders of the master;
   deletion date as a fill-in field `[____]`, not prose.
4. `showcase/cost-line-spec.md` — each element mapped to its named source field
   (table: element → field → file it lives in); honest-rounding rule (no
   flattering rounding; governance overhead visible per the D7 breakdown).
5. `showcase/team-summary-spec.md` — each section mapped to named record fields;
   plain-language style rules (founder never meets method jargon).
6. `showcase/render.sh` (or `render.py`) — generates BOTH artifacts from a repo
   path; run it against THIS repository's real data.
7. `showcase/example-cost-line.txt` + `showcase/example-team-summary.md` —
   worked examples generated by №6 from this repo (framework-v1 records are the
   richest source).

## 5. Definition of Done
**Hard criteria:**
- H1. All §4 files exist at named paths. Check: ls.
- H2. `push.sh` whitelist == pilot-design closed list, literally; negative-test
  output shows the planted files rejected. Check: diff + output.
- H3. Consent RO/RU each contain: the 4 red lines, deletion clause with fill-in
  date field, open-script clause — point-by-point table vs the EN master in
  RESULT. Check: table + grep.
- H4. cost-line-spec: all 5 elements (N/M/D/R/$X) mapped to named fields;
  team-summary-spec: all 4 sections mapped to named fields. Check: tables.
- H5. Worked examples exist and were produced by the render script from real
  repo data (command line recorded in run.log). Check: files + log line.
- H6. Scripts pass `bash -n` (or `python -m py_compile`); OFF-switch test
  recorded. Check: outputs.
**Soft criteria:**
- S1. A founder reading push.sh top-to-bottom understands what leaves their
  machine — judge: Verify agent.
- S2. The example TEAM summary would have answered lesson №2 (the CEO would
  have seen who worked, in which perspectives, where they argued) — judge:
  Verify agent.
- S3. Cost line honest — no flattering rounding — judge: Verify agent.

**Maturity target:** reviewed. Do not gold-plate.

## 6. Method Hints (non-binding)
- The render script may parse YAML/frontmatter naively (grep/sed-level) — the
  records are regular; do not import heavy libraries.
- For the worked TEAM summary use the ATOM-018 constitutional touch (executor +
  verify + human gate — a real story with a return and a gate) or ATOM-016/017;
  synthesis-conflict section may legitimately render «конфликтов не было —
  веер ещё не запускался» for pre-fan atoms; say so plainly.
- $X: use the runtime token counters found in the records; state the $/token
  assumption in the spec with a date, as an explicitly marked assumption.

## 7. Obligations & Escalation
Write `STATUS.md`, `RESULT.md` (ATOM-SPEC §6.2 discipline: DoD table, cost
block, real token estimate), `workspace/run.log`. Do NOT touch status.yaml.
Escalate (write `ESCALATION.md`, stop) on E1 — e.g. pilot-design's closed list
ambiguous about a file class, or repo records insufficient for a worked
example. Otherwise execute; no permission-asking on the normal path.
