---
atom: ATOM-072
product: Telemetry + consent RO/RU + work showcase (deliverables §4.3 + §4.5 of ATOM-007 INPUT)
status: delivered
maturity_claimed: reviewed
cost:
  units_in: ~55k
  units_out: ~18k
  unit: tokens
  wall_time: ~45m
  executor: claude-fable-5
  sub_atoms_spawned: 0
---

# RESULT — ATOM-072

## Summary
Telemetry push + negative test + OFF-switch, RO/RU consent renders, and the
two showcase specs + renderer + worked examples are complete and exercised
against real data. `push.sh` copies exactly pilot-design.md's five-item
closed whitelist (deny-by-default; `RESULT.md` reduced to its `cost:` block
only) and is a dry-run stub — no network calls, no `git push`, anywhere in
this build. `negative-test.sh` proves both the whitelist (planted `INPUT.md`
+ `secret.txt` rejected, RESULT.md business content never leaked) and the
`telemetry/OFF` switch, in a disposable `/private/tmp` sandbox, output
captured verbatim. `CONSENT.ro.md` / `CONSENT.ru.md` are substance-identical
renders of the EN master with the deletion date left as a `[____]` fill-in.
`render.sh` produced both showcase artifacts from this repository's real,
closed records — no atom in `framework-v1` has a real subtree yet (the
pilot's own atom fan, of which ATOM-072 is a member, is the framework's
first), so the worked example (ATOM-018) is honestly a pre-fan leaf: `N=0`
agents / `1` role / depth `0` / `0` verify returns is the literal, correct
reading of the O4.3 fields, not a shortfall — documented plainly in both
specs and inline in the generated files.

## Deliverables
| File | Purpose |
| :---- | :---- |
| `telemetry/push.sh` | whitelist-enforcing, deny-by-default push to `qroky/pilot-telemetry`; OFF-switch; push stubbed (dry run) |
| `telemetry/negative-test.sh` | sandbox proof: planted non-whitelisted files rejected (TEST 1) + OFF switch works (TEST 2) |
| `telemetry/negative-test-output.txt` | captured output of the above, both tests PASSED |
| `consent/CONSENT.ro.md` | Romanian render of the EN consent master, substance-identical |
| `consent/CONSENT.ru.md` | Russian render of the EN consent master, substance-identical |
| `showcase/cost-line-spec.md` | N/M/D/R/$X → named-field mapping + honest-rounding rule + $/token assumption |
| `showcase/team-summary-spec.md` | 4 INFO-007(b) sections → named-field mapping + plain-language glossary |
| `showcase/render.sh` | naive grep/awk renderer; produces both showcase artifacts from a repo path + atom id |
| `showcase/example-cost-line.txt` | worked example, generated by `render.sh` from `products/framework-v1/018-constitutional-touch` |
| `showcase/example-team-summary.md` | worked example, generated by `render.sh` from the same atom |

## DoD Self-Check
| Criterion | Result | Evidence |
| :---- | :---- | :---- |
| H1 | met | `ls telemetry/ consent/ showcase/` — all 10 named §4 files present at named paths (recorded in run.log) |
| H2 | met | `grep -n '"STATUS.md"\|"RESULT.md"\|"run.log"\|"status.yaml"\|"VERDICT.md"' telemetry/push.sh` returns exactly the 5 entries of `WHITELIST_FILENAMES`, one-for-one with pilot-design.md L42 ("STATUS.md, RESULT.md cost blocks, run.log, status.yaml, verify verdicts" — VERDICT.md is the file "verify verdicts" names, per FEV-PROTOCOL VP6); `negative-test-output.txt` shows both planted files (`INPUT.md`, `secret.txt`) absent from staging, plus a third check (RESULT.md's `## Summary` business-content string) also confirmed absent — TEST 1 PASSED |
| H3 | met | Point-by-point table below; `grep` evidence: RO — L23 open-script, L31–32 deletion+`[____]`, L37–38 four red lines; RU — L23 open-script, L31 deletion+`[____]`, L37–38 four red lines (line numbers per `wc -l`/`grep -n` on each file) |
| H4 | met | `showcase/cost-line-spec.md` "Element → field → file" table: 5/5 rows (N, M, D, R, $X); `showcase/team-summary-spec.md` "Section → source fields" table: 4/4 rows (roles+contribution, lens map, synthesis conflicts, verify returns) |
| H5 | met | `showcase/example-cost-line.txt` + `showcase/example-team-summary.md` exist; produced by `cd showcase && ./render.sh /Users/ghcernei/Work/rpf ATOM-018` (exit 0) against `products/framework-v1/018-constitutional-touch/*` — command line recorded in `workspace/run.log` |
| H6 | met | `bash -n` clean on `telemetry/push.sh`, `telemetry/negative-test.sh`, `showcase/render.sh` (recorded in run.log); OFF-switch test = TEST 2 in `negative-test-output.txt` — exit 0, loud "TELEMETRY IS OFF" message, zero files staged |
| S1 | self-reviewed | `push.sh`'s top third is a plain-language block comment stating, before any code, exactly which 5 file kinds leave the machine, that `RESULT.md` is cut down to its cost block only, where the OFF switch lives, and that the push step is a stub with no network I/O — a founder reading top-to-bottom meets the whitelist and the deny-by-default rule before a single line of bash runs — judge: Verify |
| S2 | self-reviewed | The example TEAM summary names the task owner's role and one-line contribution, the independent check's round/verdict/fix-rounds, the human's plain-language decision, the lens map (opt-out, single perspective), and states plainly that no cross-perspective conflict exists yet because no fan has closed — a CEO reading it sees who worked, under which perspective, and why there's no dispute to report; the multi-descendant branch (roles list, synthesis-conflict outcomes) is implemented and will populate identically once a real fan closes — judge: Verify |
| S3 | self-reviewed | `$X` uses the *larger*, measured real counter (177,951 tokens) over the smaller pre-close estimate (~152,000) — the opposite of a flattering choice; the dollar amount rounds up to the next cent, never down; the $/token rate and its date are printed inline, never hidden; the four-type governance breakdown is preserved in the spec's honest-rounding rule even though this worked example's breakdown is 100% `execute` (no fan overhead yet, stated plainly) — judge: Verify |

**Maturity target: reviewed. Not gold-plated** — `render.sh`'s multi-descendant path (real fan, real `SYNTHESIS.md`) is implemented but not exercisable against real data yet, per its own honest comment; it is not stubbed out, only untested against a fan that has not happened in this repo.

## Consent point-by-point table (H3)

| Element (EN master, pilot-design.md) | CONSENT.ro.md | CONSENT.ru.md |
| :---- | :---- | :---- |
| Red line 1 — no turnkey production code | "nu livrăm cod de producție gata de utilizare (turnkey)" | "не поставляем готовый (turnkey) продакшн-код" |
| Red line 2 — no investment guarantee | "nu garantăm investiții" | "не гарантируем инвестиции" |
| Red line 3 — no exclusivity | "nu cerem exclusivitate" | "не просим эксклюзивности" |
| Red line 4 — no team hiring/replacement | "nu angajăm și nu înlocuim echipa dumneavoastră" | "не нанимаем и не заменяем вашу команду" |
| Deletion clause, fill-in date (not prose) | "**Ștergere:** ... la data de **[____]** (dată fixată în scris la semnare — implicit: sfârșitul pilotului + 30 de zile), sau mai devreme, la cererea dumneavoastră" | "**Удаление:** ... удаляется **[____]** (дата фиксируется письменно при подписании — по умолчанию: конец пилота + 30 дней), либо раньше — по вашему запросу" |
| Open-script clause | "Scriptul care trimite aceste fișiere este deschis (open) — puteți citi exact ce trimite" | "Скрипт, отправляющий эти файлы, открыт (open) — вы можете прочитать построчно, что именно он отправляет" |

## Decisions Made by Executor
1. `RESULT.md` whitelist entry means the frontmatter `cost:` block **only** — never the whole file — per pilot-design.md's literal wording ("RESULT.md cost blocks", plural qualifier), since RESULT.md's Summary/Deliverables/Decisions sections are exactly where business content would live. `push.sh`'s `copy_result_cost_block()` implements this with a naive awk block scan (cost: at column 0 → next column-0 key or closing `---`).
2. `negative-test.sh` folds the OFF-switch proof (H6) into the same script/output file as the whitelist proof (deliverable #2 names one script + one output file); TEST 1 and TEST 2 are clearly labelled sections rather than a second script pair.
3. Sandbox location: `/private/tmp/qroky-telemetry-negtest.XXXXXX` (mktemp), removed by a `trap cleanup EXIT`; the same trap also removes any `telemetry/OFF` marker the test plants, so the shipped kit defaults to telemetry ON.
4. `push_to_remote()` is a hard stub (no git, no network) for the whole of this build, not only inside the test — the orchestrator's instruction ("no network pushes, no real git push") and E5 (sandbox-only scope) both apply to normal operation, not only to testing.
5. $/token rate for `$X`: an explicit, dated placeholder ($8.00/M tokens, 2026-07-07) — `pricing/pricing-ladder.md` is outside this atom's named input scope, so no real contracted rate was available to read; `cost-line-spec.md` marks this as a must-replace-before-a-founder-sees-it assumption.
6. Honest-rounding source preference: when both a `RESULT.md` frontmatter estimate and a `status.yaml` "executor real" counter exist for the same atom, `render.sh` uses the real counter — even though it is larger (177,951 vs. ~152,000 for ATOM-018) — because "prefer measured over estimated" cannot also mean "prefer whichever is smaller."
7. Worked-example atom: ATOM-018 (framework-v1's constitutional touch), chosen because it is the richest closed record inside this atom's named "real data" sources (`status.yaml`, its own `RESULT.md`, `GATE-011`) and matches the Method Hints' preferred shape (executor + independent Verify + human gate); it does not have a Verify *return* (round 1 accept), and the Method Hints' parenthetical mentioning "a return" is non-binding — no return was fabricated to match it.
8. TEAM summary's human-decision cell derives one plain sentence from `status.yaml`'s closure note rather than quoting the note verbatim, per `team-summary-spec.md`'s own plain-language rule (the raw note is dense with gate/criterion ids that would otherwise leak method jargon into a founder-facing artifact).

## Deviations & Known Limitations
- No parent atom with a real, closed subtree (`total_descendants` > 0) exists anywhere in this repo's named data sources yet — `render.sh`'s multi-descendant TEAM-summary branch (reading an actual `SYNTHESIS.md`, listing per-descendant roles) is written but has never run against real data; the single-atom branch it falls back to is fully exercised. This is a repo-state fact, not a shortcut — flagged inline in both the code and both spec documents.
- The $8.00/M-token rate is a stated placeholder, not sourced from `pricing/pricing-ladder.md` (outside this atom's named input scope). Must be replaced with the real contracted rate before ATOM-007 setup reaches a founder.
- `render.sh`'s `## Summary` extraction takes the first sentence only (split on the first `.`); an atom whose first sentence is unusually long or abbreviation-heavy (e.g. "e.g." inside it) would split early. Acceptable for this repo's writing style; worth a look if role specs elsewhere favor heavy abbreviation.
- `push.sh`'s repo-wide `find` scan has no exclusion list beyond `.git/` and its own staging directory; a founder's repo with an unusually large non-Qroky subtree (e.g. a vendored `node_modules/`) would be slow, not wrong — deny-by-default still holds, just at a `find`-walk cost. Not a defect for a daily cron-scale push; worth revisiting only if founders report multi-minute runs.

## Handoff
Verification mode: independent (slim VP20). Verify receives: DoD (INPUT.md
§5) + the ten deliverable files listed above + this RESULT.md and
STATUS.md. Every hard criterion above is script-checkable (bash -n, grep,
ls, diff against pilot-design.md's quoted closed-list line, and re-running
`negative-test.sh` / `render.sh` directly) — no judgment rests on digested
text, so the slim package condition (VP20.1) holds; this RESULT.md and the
two spec documents name every file this product cites, satisfying VP20.2.
