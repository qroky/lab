# NARRATIVE — ATOM-150 (windows-path-v1)

**12.07.2026 — старт вне очереди.** Волна №1 разослана в день G2 — и первый же получатель на Windows (INFO-046). Это не сбой плана, а его работа: порядок INFO-043 строился ради живого фидбека, фидбек пришёл за часы. Разведка перед формулировкой: кит УЖЕ деградирует честно без launchd (install.sh:906/1188 — command -v guard, сообщения L_*_NO_LAUNCHD), значит v1 = не порт, а абстракция планировщика (launchd/systemd/cron) + WSL2-quickstart + Linux-прогон харнеса. Нативный PowerShell — NOT-DOING до отдельного решения. macOS-байт-паритет — жёсткое требование H4: у второго получателя волны Mac, его путь не смеет измениться.

**12.07.2026 — verify RETURN r1 → фикс F1 → ACCEPT; v0.5 оттегирован.** Слепой мутационный проверяющий (5 мутаций, все фальсифицированы как должно) поймал ОДИН blocking (F1): `_sched_backend` ставил глобал SCHED_BACKEND внутри `$()`-subshell → install-state.json всегда писал `sched_backend: ""` при верном диспатче (тот использует SCHED_USED). Для фреймворка «без тихих дыр» — дефект. Фикс координатора одной строкой (`SCHED_BACKEND="$SCHED_USED"` в _sched_run, коммит ca6f48b→6c9fd87 после rebase на ack). Перепроверка F1: propagation доказан пробой, порядок state_commit-после-_sched_run подтверждён (1218/1231/1258/1362 за 1062/1344), macOS байт-паритет НЕ затронут (диспатч по-прежнему на SCHED_USED, launchd/plist не тронуты), freeze-check OK, bash -n OK. macOS-паритет как gate — PASS (cmp+md5 доказано проверяющим). NOT-DOING соблюдён (нет PowerShell-порта, кит не ставит WSL, WSL1 отвергнут).

**Тег v0.5 опубликован** (Windows via WSL2 + Linux first-class; Mac install unchanged byte-for-byte). maturity = reviewed; validated = после живого WSL2-прогона получателя волны №1 (INFO-035/INFO-046). НОТА координатору: shipped `dry-run.sh` держит ATOM_WORKSPACE_SRC на уехавший при SEPARATION путь products/.../131/ — внутренний dev-артефакт, пользовательский путь не трогает; будущий cleanup, не дефект атома.
