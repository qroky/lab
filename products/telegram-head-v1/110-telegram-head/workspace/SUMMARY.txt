dry-run.sh — Telegram head, summary of all scenarios
Generated: 2026-07-11T09:28:33Z

PASS — 1-parity: press -> inbox -> record with full question + verbatim label; session record differs in channel line only (diff non-empty)
PASS — 2-closed-session: press with no session persisted; consumed exactly once across two wakes; SIGKILL inside the write window left complete-or-nothing; crashed pass's lock stolen IMMEDIATELY by the next pass (M2) and the press re-delivered, not lost
PASS — 3-physics: no listening ports on a live pass; restart replays nothing (offset survives); cadence 30s; digest calendar = profile time, daily
PASS — 4-foreign-chat: foreign message AND foreign button press: zero replies, zero events, flagged + quarantined
PASS — 5-risk-word: no buttons on the risk item; button-style reply rejected + re-asked; only the typed word produced a record (verbatim)
PASS — 6-digest: four sections + running/pending content; planted already-signaled gate rendered as status line (no duplicate alarm); 3-line changelog on new tag; once per day
PASS — 7-events-feed: heartbeat-side events instant via the shared helper; beats ride the contour at levels 2/3, level 1 = gates only, no re-sends
PASS — 8-ack-promise: ack within the polling budget; promise recorded in inbox BEFORE work; SIGKILL between ack and result -> promise survived and was answered after wake, N kept
PASS — 9-quiet-hours: night events queued (0 sent), user acked even at night, morning flush delivered blocker FIRST
PASS — 10-status-router: /status + «что в работе» render from status.yaml in <=1 message; router asked exactly one question then filed a task-proposal; the bot executed nothing
PASS — 11-secrets: token flowed through every API call (stub-verified) yet appears nowhere: log, state, records, sent payloads, committed files, transcripts all clean
PASS — 12-long-label: 111-byte-class label rides as button text; callback_data <=64 bytes (stub enforces the real API cap and its rejection was proven live); recorded answer = full label verbatim via registry resolution
PASS — 13-digest-retry: missed daily fire recovered by the next listener pass after DIGEST_TIME (nothing before it); waiting section coherent — no «решений не ждём» beside real waiting items
PASS — 14-masked-error: forced token-bearing curl errors x3: every log line masked to bot****LEAK, zero raw-token traces; ladder queued the event and the next pass delivered it
PASS — 15-double-press: double-tap, answered-gate press, and two malformed callbacks all politely declined (4 replies), zero records, offset advanced, every pass rc 0, contour healthy after — the R2-1 poison pill is gone
PASS — 16-migration: bound v1 state seeded its registry on the first pass with zero sends, zero re-binds, offset and narrative offsets intact; seed idempotent
PASS — 17-cross-routing: second workspace registered idempotently; events labeled per origin (PROJECT_NAME override honored); button press routed the decision record to the ORIGIN workspace only; dead origin degraded honestly to local
PASS — 18-status-filter: /status renders every registered project labeled in one message; name filter isolates one project without leaking others; unknown name declined honestly
PASS — 19-merged-digest: one merged message: per-project sections with their own atoms/gates/spend, empty project one line, pending gate confined to its origin section, numeric total, signaled events as status lines
PASS — 20-reask: no-project text got exactly ONE mechanical re-ask (buttons incl. «общий»); the press routed the original text and skipped a second clarify; a named text routed directly and its clarify round-trip landed the proposal in the named project
PASS — 21-deadpath: dead registry path: rc 0 everywhere, live projects unaffected, one honest ⚠ line in /status and in the digest, log flagged, unregister clean + idempotent

Totals: 21 passed, 0 failed
Sandbox: /var/folders/pd/mlvnqxbj7tj0h1qb5qv78_l00000gn/T/qroky-tg-dry.6ejLNV (removed on exit)
Budget line: harness burns ~0 tokens (no LLM — stubbed), ~60s wall time, 0 network calls
