# VERDICT — ATOM-105 (clean-run readiness, kit v0.3.1)

**ACCEPT** (0 blocking / 0 major / 2 minor)

Blind Verify, L-tier targeted. Date: 11.07.2026. Mandate: /ATOM-105-LAUNCH.md.
Ни одному заявлению исполнителя не доверял — всё перепроверено запуском/кодом/git-историей.

## Что перезапускал сам (не по транскриптам)

1. **Kit harness** `distribution/dry-run.sh` — свой прогон: **17/17 PASS, rc 0**
   (совпадает с заявленным).
2. **Head harness** `runtime/claude/telegram/dry-run.sh` — свой прогон:
   **21/21 PASS, rc 0**. Важно: head dry-run.sh **не изменён** относительно
   git HEAD (git status чистый) — 21/21 на нетронутых ассертах со старыми
   ru-литералами и есть доказательство байт-совместимости дефолта.
3. **Мутационные тесты фальсифицируемости** (во временной копии, репо не тронуто):
   - M1: ru `L_UPDATE_ASK_CONFIRM` подменён английским текстом → сценарий 14
     **FAIL** (16/17). Языковые ассерты не пустые.
   - M2: из `cmd_uninstall` убран `rm -f "$STATE_FILE"` → сценарий 15 **FAIL**
     (re=0/0/1/0: reinstall не прошёл «как первый»). Clean-slate leg содержателен.
   - M3: возврат до-фиксного поведения pointer (удаление без проверки
     have_state+содержимого) → сценарий 15 **FAIL** (noop=0/0). Заявленный
     пойманный баг «осиротевший чужой workdir-pointer» реально дискриминируется
     харнесом; фикс — install.sh:1472–1479 (удаление только при
     `cat pointer == WORKSPACE_DIR`).

## Проверки по коду и git-истории

4. **--uninstall безопасность** (install.sh:1391–1493, прочитан целиком):
   - (а) workdir пользователя не удаляется ни на одном пути: rm-цели — только
     plist'ы под `~/Library/LaunchAgents`, SKILL.md/маркер-блок под `~/.claude`,
     `~/.qroky`, TOKEN_FILE/STATE_FILE (оба заданы только при have_state=1 из
     `cd && pwd`, т.е. непустые абсолютные), WORKDIR_POINTER (якорен на
     $SCRIPT_DIR). Ни одного rm на $WORKSPACE_DIR/$candidate; переменной
     $WORKDIR в rm-путях нет; `set -euo pipefail` активен — unset-переменная
     аборт, пустой WORKSPACE_DIR недостижим (cd fail → abort).
   - (б) токен: `L_UNINSTALL_TOKEN_WARN` печатает только путь; затем `rm -f`.
     Единственное чтение TOKEN_FILE в файле — install.sh:778 (step_telegram,
     легитимный reuse при установке), в uninstall-ветке чтения нет.
   - (в) SKILL.md удаляется только при `grep -qF "GATE-028"`; чужой файл →
     `L_UNINSTALL_FOREIGN_SKILL`, остаётся (подтверждено и сценарием 15,
     foreign=1/1). CLAUDE.md — только маркер-блок через awk.
   - (г) каждый rm предварён `L_UNINSTALL_STEP` (печать ДО выполнения) — да,
     во всех пяти блоках.
   - (д) чистая машина: have_state=0, DONE_LIST пуст → `L_UNINSTALL_NOOP`,
     return 0 — подтверждено кодом и no-op leg сценария 15 (+M3 доказал, что
     ассерт не вакуумный).
5. **Факт-проверка «prompt никогда не был EN-hardcode»: ПОДТВЕРЖДЕНА.**
   `git log -S 'Apply this update now'` + `git grep` по всем коммитам
   distribution/: строка существует ТОЛЬКО в lang/en.sh
   (`L_UPDATE_ASK_CONFIRM`) и в grep-ассерте dry-run.sh — с самого a097c59
   (v0.1). install.sh во всех версиях вызывает L_UPDATE_ASK_CONFIRM
   (a097c59:650, ff981c9:697). `is_affirmative` принимает
   yes/y/da/d/да/д/1 с v0.1 (a097c59:67–70). Предпосылка мандата
   (install.sh:887 EN-hardcode) не подтверждается историей; заявление
   исполнителя — истинно, факт для человека корректен.
6. **T_*-каталоги**: en/ro/ru — по 46 ключей, наборы идентичны (diff пуст).
   lib.sh:77 дефолт `LANGUAGE="ru"`, lib.sh:84 валидация с фолбэком ru —
   отсутствие LANGUAGE= → ru. Kit пишет `LANGUAGE="${ANSWER_LANGUAGE:-ru}"`
   в profile.conf головы (install.sh:610).
7. **Байт-паритет** (сужено, см. ниже): 3 старых ru-литерала из HEAD-версии
   handler.sh найдены байт-в-байт в новом lang/ru.sh (и исчезли из
   handler.sh — вынос, не переписывание); head-харнес не изменён и зелёный;
   kit dry-run.sh diff против HEAD = 171 вставка (2 новых сценария) и ровно
   1 удаление — путь workspace 104→105. **Старые ассерты kit-харнеса не
   ослаблялись**, 15 прежних чеков прошли на новом install.sh.
8. **README ×3**: раздел есть во всех трёх (en:193 «Starting from a clean
   slate», ro:203 «Pornire de la zero», ru:201 «Начать с чистого листа»),
   содержание совпадает по смыслу (те же 4 гарантии: печать до выполнения,
   workdir не трогается, токен с предупреждением, no-op на чистой машине).
9. CHANGELOG.md: запись v0.3.1 есть (строка 8) — INFO-024 соблюдён.

## Находки

- **minor-1**: provenance-маркер machine-wide SKILL.md — просто подстрока
  «GATE-028» где угодно в файле (`grep -qF`). Любой чужой файл, упоминающий
  GATE-028, будет удалён. Соответствует букве мандата («наша provenance»),
  но маркер грубый; при случае заменить на полный маркер-блок.
- **minor-2**: несимметричные дефолты локали — uninstall/подкоманды без
  state фолбэчат на en (`${ANSWER_LANGUAGE:-en}`, install.sh:1403), голова —
  на ru. Обосновано исполнителем (локаль неизвестна до q1) и
  задокументировано в RESULT, но человек на «чистой» машине с ru-ожиданием
  увидит EN no-op строку. Не блокирует.

## Что сузил по конверту

Полный byte-diff всего вывода kit против прогона HEAD-версии харнеса не
делал: заменил тройной уликой — (а) head-харнес не изменён и 21/21, (б)
спот-чек 3 литералов байт-в-байт, (в) diff kit-харнеса показывает ноль
изменённых старых ассертов (только +2 сценария и путь workspace). Паритет
decision-записей/дайджеста покрыт нетронутыми ассертами сценария 7 (kit) и
дайджест-сценариями головы. Мутации — по одной на ключевой ассерт-класс
(язык, clean-slate содержательность, pointer-фикс), не по каждому ассерту.

## Расход

~18k токенов из ~25k конверта (в пределах; E4 не потребовался).
