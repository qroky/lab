# VERDICT — ATOM-106 (Reinstall Path, kit v0.3.2) — Blind Verify (targeted, L-tier)

**ACCEPT.**

Verifier: blind, targeted (INFO-037 mutation falsification включена). Дата: 11.07.2026.
Мандат: /ATOM-106-LAUNCH.md (DoD 1–7, включая fold-in INFO-041 и INFO-042).

## Что перезапускал сам

1. **Kit harness** `distribution/dry-run.sh` — собственный прогон: **20/20 PASS**
   (совпадает с заявленным).
2. **Head harness** `runtime/claude/telegram/dry-run.sh` — собственный прогон:
   **21/21 PASS**. Плюс `git diff --name-only v0.3.1..HEAD -- runtime/claude/telegram/`
   = 0 файлов — голова действительно не тронута.

## Мутационная фальсификация (3 мутации, временные копии, репо не тронуто)

| Мутация | Что сломано | Результат харнеса | Вердикт |
|---|---|---|---|
| A | `_reinstall_gate` → `return 0` (до-фиксное поведение, диалога нет) | **16/20** — упали ровно 3, 15, 16, 17 (диалог + recovery) | детектируется |
| B | убрана зачистка `.git/modules/framework` (одна из трёх purge-веток) | **13/20** — упали 3, 5, 7, 10, 12, 15, 17: каждый перезаход в занятую папку умирает (framework не восстановлен, финал не показан) | детектируется |
| C | `step_machinewide` → ранний return (always-on выключен, «возврат q9-мира») | **13/20** — сценарий 12 упал жёстко (claude_files=0/0, home_files=1) + каскад 2/3/15/16/17/18 | детектируется |

Ни один мутант не выжил; тестов, не падающих на сломанном коде, не найдено.

## Целевые проверки по DoD

- **Безопасность «переустановить» (по коду, вся ветка):** оба `rm -rf` в
  reinstall-пути прибиты к `$WORKSPACE_DIR` с литеральными суффиксами
  (`/framework`, `/.git/modules/framework`); `_reinstall_gate` охраняет пустой
  `WORKSPACE_DIR` (`[[ -n ... ]] || return 0`), скрипт под `set -euo pipefail`;
  `_framework_purge` вызывается ТОЛЬКО через gate. Живое (install-state,
  CLAUDE.md, .qroky/, mission/, decisions/, atoms/, токен, профиль) в
  rm-целях не фигурирует; сценарий 3 ассертит токен по mtime, профиль по md5,
  state-diff пуст. «Отмена»/Enter/EOF → `exit 0` до записи pointer/state,
  харнес доказывает пустой tree-diff и state-diff. «Обновить» без
  install-state = `L_REINSTALL_UPDATE_NEEDS_STATE` + переспрос (код,
  строки 292–297), не крах.
- **Байт-паритет чистого случая:** негативный grep содержательный —
  `PARITY_CLEAN` ловит те же фразы («already carries a Qroky install» /
  «looks like an orphaned clone»), которые в сценариях 3/16/17 обязаны
  матчиться >0; не-вакуумность встроена в сам PASS-гейт сценария 16.
- **Инвентарь 8:** `IV-POINT:9` в install.sh отсутствует (grep=0, max=8);
  харнес ассертит «of 9»-остатки = 0 и заголовки «of 8»; README ×3 несут
  «восемь вопросов» + честный абзац «здесь был девятый»; сценарий 12 (мой
  зелёный прогон): без вопроса, ровно 2 файла в ~/.claude, скилл
  byte-identical вендоренному, чужой CLAUDE.md дописан — не затёрт,
  след-строки в финале и README ×3.
- **INFO-041, три точки:** `L_FINALE_NEW_SESSION_NOTE` в finale();
  `L_TG_HELLO_TEXT` содержит буквальный маршрут с реальным путём (%s =
  WORKSPACE_DIR), префикс «Я на связи…» сохранён (старые greps живы —
  сценарий 11 зелёный); `L_MARKER_SESSION_NOTE` в ОБОИХ маркер-блоках
  (строки 578 и 1304 install.sh, внутри маркерных скобок → uninstall
  вычищает целиком; сценарий 18 ассертит fence-включение). Всё ×3 локали
  (лок-паритет 15 новых L_-функций — ассерт харнеса).
- **Вендоренный скилл:** `git diff v0.3.1..HEAD -- runtime/claude/skill/qroky/SKILL.md`
  = ровно 2 изменения: I3-абзац переобоснован (Amended INFO-042, lex
  posterior) + 1 строка в §4-таблицу. Строка «GATE-028» физически сохранена;
  uninstall-провенанс-grep (`grep -qF "GATE-028" "$mw_skill"`, строка 1590)
  жив. Больше в файле ничего не менялось.
- **interview-audit.md:** только рекомендации («решает CEO» — заявлено
  явно), все 8 вопросов покрыты, 5–8 (наружу) к снятию НЕ рекомендованы,
  единственный будущий кандидат q2 оставлен как есть. В полномочиях.
- **CHANGELOG v0.3.2:** присутствует, user-benefit язык, все шесть пунктов
  соответствуют доставленным фактам (диалог, recovery, orphan, hint,
  new-session note, «одним вопросом меньше / eight questions»).

## Находки

1. **[NOTE / minor]** Под мутацией B «тот самый сырой fatal» на экране не
   виден (git-шум по дизайну уходит в лог, не на экран) — сценарии падают
   через функциональные ассерты (framework не здоров, финал отсутствует).
   Детекция полная, требование фальсификации выполнено; фиксирую лишь
   расхождение с буквой формулировки мандата verify-инструкции.
2. **[NOTE / minor]** Мутация C каскадно валит и сценарии 2/3 (kill-resume и
   диалог зависят от machinewide-шага state-машины) — чувствительность
   харнеса выше необходимой, дефектом не является.
3. Дефектов классов CRITICAL/HIGH/MEDIUM не найдено. Заявления RESULT.md
   выборочно фальсифицировались — расхождений нет.

## Сужено по конверту

- Байт-паритет случая (в) принят по харнесному ассерту (PARITY_CLEAN +
  зелёные сценарии 1/5 без правок сути), без независимого до-фиксного
  эталонного прогона.
- ro-локаль проверена grep-полнотой (лок-паритет 15 функций + след-строки),
  без отдельного ro-прогона диалога (харнес гоняет en+ru ноги).
- uninstall-ветка перечитана по коду и сценариям 15/18, без ручного live-прогона.

## Расход

~26k из ~30k (2 полных прогона харнесов + 3 мутационных прогона + код-ревью
ветки + greps). E4 не потребовался.
