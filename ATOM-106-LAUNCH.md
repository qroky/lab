# ATOM-106 — Reinstall Path (kit v0.3.2)

- **Дерево:** A (distribution kit), carry-over зачётного прогона №1
- **G1:** INFO-040 (спецификация CEO дословно; цикл фикса пред-авторизован
  INFO-035)
- **Роль:** pilot-toolsmith (тёплый)
- **Конверт:** ~145k (executor ~110k, verify ~30k targeted blind) —
  поднят с ~110k при fold-in INFO-041 (заминка №3), затем с ~125k при
  fold-in INFO-042 (заминка №4: q9 исключён + аудит интервью).
  ⚠️ INFO-019: конверт — flagged practice; исчерпание → честный E4.
- **Релиз:** тег v0.3.2 + CHANGELOG (INFO-024).

## Зачем (два потолка)

Ближний: пользователь, запускающий установку в непустом месте (повторно,
после сбоя, после битого self-update), должен получить понятный диалог, а
не сырой git fatal — CEO встретил именно его. Дальний: та же ветка = штатное
восстановление инстанса, опора надёжности self-update канала.

## Мандат (DoD)

1. **Три случая точки входа** (детект по построению: framework/ =
   пересоздаваемая read-only копия; данные = qroky/ рядом, живые файлы):
   - (а) framework/ + данные рядом → диалог «переустановка: данные не
     трогаю, framework пересоздам [переустановить/обновить/отмена]»;
     «переустановить» = снос framework/ + свежий клон + идемпотентный
     прогон install-state сверху — живое (workdir, профиль, токен,
     привязка) не трогается; «обновить» = маршрут --apply-update;
     «отмена» = выход без следа.
   - (б) framework/ без данных рядом → «осиротевший клон, пересоздать?» →
     да = снос + чистая установка; нет = выход.
   - (в) чистого места → обычная установка (байт-в-байт нынешнее
     поведение — паритет харнесом).
2. **Финал uninstall** ссылается на этот путь: «для переустановки просто
   запусти install заново — данные останутся» (все 3 локали).
3. **Recovery-семантика:** сценарий «битый framework/ после прерванного
   self-update» проходит через (а)/(б) до рабочего состояния без потери
   живого.
4. **Харнес:** сценарии на (а)×3 ответа, (б)×2, (в)-паритет, recovery;
   ассерты фальсифицируемые + мутационная проверка (INFO-037) в verify.
5. **Локали:** все новые строки через L_*, en/ro/ru, «да/da/yes».
6. **Видимость свежего жеста (INFO-041, заминка №3):** среда читает
   контекст при старте сессии — свежеустановленный machine-wide жест
   невидим уже открытым окнам. Кит говорит об этом в трёх местах пути:
   - (а) финальный экран: явная строка «открой НОВУЮ сессию Claude Code —
     установленное видят только новые чаты»;
   - (б) Telegram-welcome (hello) дублирует буквальный маршрут: новый чат →
     папка → фраза;
   - (в) маркер-блок в CLAUDE.md: при упоминании qroky без доступного
     скилла ассистент предлагает «возможно, установлен после старта
     сессии — открой новый чат», а не голое not found.
   Все три — во всех локалях; харнес проверяет присутствие строк в
   финале/hello/маркере (обе копии маркера: workdir и machine-wide).
7. **Вопрос №9 исключён (INFO-042, заминка №4; supersedes GATE-028):**
   machine-wide жест ставится ВСЕГДА — те же ровно два файла, идемпотентно;
   периметр не расширяется. След вместо спроса: финальный экран +
   uninstall-док несут строку «отвечает на qroky в любой сессии Claude
   Code; удаляется целиком одной командой» (все локали). Интервью: 9 → 8
   вопросов (инвентарь, «N из 8», state, харнес-инвентарь). Вендоренный
   скилл: I3-исключение переобосновать (Amended, INFO-042 — «всегда, со
   следом», вместо q9 opt-in); авторскую копию правит родитель. ПЛЮС
   аудит-отчёт по оставшимся 8 вопросам фильтром CEO: «есть ли у
   пользователя основание для ответа?» — по вопросу: основание есть/нет,
   рекомендация (спрашивать / решение производителя со следом); opt-in
   неприкосновенен для отдающего наружу (телеметрия, бэкап,
   heartbeat/сеть). Отчёт = файл в workspace, решения по нему — CEO.

## NOT-DOING

Bootstrap-скрипт (кандидат SEPARATION-дерева, INFO-040); изменение
интервью; любые правки головы.
