# GATE-031 — «го» на SEPARATION (ATOM-130)

- **Тип:** GATE (G1)
- **Дата:** 11.07.2026

## Формулировка CEO (дословно)

> SEPARATION — го: исполняй по плану из консолидированного пакета
> (framework = чистый продукт, lab = публичная фабрика, dist-manifest,
> sparse-вендоринг). Мою HUMAN-TASK на ревизию decisions поставь мне
> отдельно.

## Объём «го»

1. **ATOM-130 (SEPARATION)** по плану INFO-036 блок 4 + carry-over'ы:
   dist-manifest (явный whitelist путей дистрибутива), sparse-вендоринг
   инсталлера по manifest, qroky/lab = публичная фабрика (вся стройка:
   атомы, decisions, NARRATIVE, TASKS), framework = чистый продукт;
   bootstrap-скрипт (INFO-040 + «нормальная команда uninstall»).
   Создание публичного репо qroky/lab и push туда — входит в объём этого
   «го» (план назвал три дома явно).
2. **Ревизия decisions CEO** — отдельная HUMAN-TASK, идёт ПАРАЛЛЕЛЬНО
   сборке (слово CEO «поставь мне отдельно», не блокером): содержимое
   decisions уже публично в истории framework; находки ревизии
   применяются правками в lab после.
3. **Констрейнт истории (важно, записано до старта):** git-история
   framework НЕ переписывается — на ней висят release-теги v0.1.2…v0.3.2 и
   self-update канал живых инстансов; чистка = HEAD going forward. Полное
   вычищение чувствительного из истории (если ревизия CEO потребует) —
   отдельное решение CEO с отдельной ценой (перепубликация, ломка тегов).
4. После доставки: **перезачёт №2** (install.sh меняется — INFO-035),
   затем ATOM-120 README → verify.sh → волна №1 (порядок INFO-043).
