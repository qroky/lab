# GATE-028 — G1: запуск ATOM-104 (installation journey, фикс возврата G2)

- **Тип:** GATE (конституционный, G1)
- **Дата:** 10.07.2026

## Ответ CEO (дословно, DR5)

> go

## Покрытие

«Go» покрывает план из GATE-027 (5 пунктов): (1) дал ключ → бот немедленно
пишет (прошивка reviewed-головы ATOM-110 в kit); (2) карта пути + «N из 8»;
(3) финал = copy-paste блок + строка VS Code; (4) секция «первые 5 минут»;
(5) пере-прогон G2 CEO. Новый конверт ~250k (E4 дерева A признан GATE-027).

**Открытый вопрос machine-wide жеста — ОТВЕЧЕН вдогонку (дословно):**

> да, спрашивать при установке

Следствия: интервью расширяется владельцем закрытого списка до **девяти**
пунктов — вопрос 9: сделать фразу доступной из любого чата на этой машине
(opt-in, честная строка «запишем два файла в ~/.claude»). «Да» → копия
скилла в `~/.claude/skills/qroky/` + маркер-блок в `~/.claude/CLAUDE.md`
(идемпотентно, только эти два файла). «Нет» → жест только в папке проекта.
Правило I3 («не писать в ~») получает записанное исключение этим гейтом:
machine-wide строго по явному opt-in ответу владельца машины — обе копии
правила (скилл автора + вендоренный runtime/claude/skill) правятся
синхронно с провенансом GATE-028.

## Физика «бот пнул» (закрытая деталь формулировки)

Telegram не позволяет боту писать первым: чтобы бот «пнул», владелец один
раз жмёт Start у своего бота. Поэтому шаг интервью расширяется (внутри того
же вопроса 5, инвентарь = 8): дал токен → «теперь откройте бота и нажмите
Start» → установщик ловит chat_id через getUpdates (это же даёт привязку
H4) → бот немедленно отвечает «я на связи». Таймаут с честным «пропустить,
подключить позже» — застрять невозможно.
