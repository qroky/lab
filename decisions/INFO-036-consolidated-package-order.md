# INFO-036 — Консолидированный пакет улучшений (Codex-ревью × дайджест №2 × harness-уроки)

- **Тип:** INFO (пакетный приказ CEO; вводит МОРАТОРИЙ на новые правила до freeze v0.2)
- **Дата:** 11.07.2026
- **Источник:** файл, сведённый внешним советником и CEO, доставлен CEO
  в живом диалоге с командой «Исполни то, что есть в этом файле»
  (иерархия INFO-029: файл = данные; команда — живая, владельца).
  Ниже — содержимое файла дословно.
- **Downstream записи:** INFO-037 (мутационная фальсификация verify),
  INFO-038 (контекст-диета + heartbeat-дайджест), INFO-039 (уроки OpenAI
  harness-engineering: 4 touch-кандидата + 2 NOT-DOING); поправка
  ATOM-120-LAUNCH (блок 2); TASKS — очередь touch до №26, план блоков 3–4
  с конвертами (CEO на го по порядку исполнения).
- **Конверты блоков 3–4 (по калибровкам INFO-019, деревья маленькие):**
  freeze v0.2 ≈120k; scripts/verify.sh ≈150k; shell-tightening ≈150k;
  release-readiness ≈30k; SEPARATION (dist-manifest + sparse-вендоринг +
  qroky/lab) ≈250k + новый зачётный прогон по INFO-035. Итого ≈700k,
  «го» — по шагам порядка, не пакетом.

---

## AMENDED 11.07.2026 — правка порядка (решение CEO, дословно)

> Правка порядка (решение CEO): SEPARATION (блок 4) исполняется ДО волны №1,
> сразу после первого зачётного прогона — пользователи с первой раздачи
> получают чистый инстанс без нашей рабочей истории (decisions/products/TASKS
> не едут: sparse-вендоринг по dist-manifest). Судьба фабрики: qroky/lab
> остаётся публичным (журнал = доказательство), но до переезда — ревизия
> decisions глазами CEO на предмет чувствительного (суммы/имена/цитаты —
> редактировать или в приватный слой); HUMAN-TASK на ревизию — CEO. После
> SEPARATION — короткий повторный зачётный прогон (INFO-035), затем
> волна №1. Блок 3 — после волны №1, как было.

Новый порядок исполнения: зачётный прогон №1 (v0.3.1 как есть) →
HUMAN-TASK: ревизия decisions CEO (до переезда в lab) → **блок 4 SEPARATION**
(≈250k) → короткий повторный зачётный прогон №2 → волна №1 → блок 3
(freeze v0.2 → verify.sh → tightening → readiness) → волна №2.
Примечание агента (не решение): ATOM-120 (витрина/handoff) логично идёт
после SEPARATION — README пишется уже для чистого product-репо; его «го»
остаётся отдельным словом CEO.

---

Контекст: внешняя рецензия Codex (бриф у CEO), дайджест №2, разбор OpenAI harness-engineering и ответ про вендоринг knowledge/ — сведены советником и CEO в один пакет. Порядок исполнения жёсткий, потому что часть шагов обнуляет зачётный прогон, а часть от него зависит.

## Режим (действует с этого сообщения)

**Мораторий на новые правила:** до исполнения freeze v0.2 — никаких новых INFO/touch-кандидатов/конституционных практик, кроме фиксаций уже действующих практик (блок 1). Диагноз Codex совпал с нашим: риск не в нехватке правил, а в их весе. Cleanup \> рост.

## Блок 1 — фиксации потерь (micro, сейчас, зачёт не трогают)

1. **INFO: мутационная фальсификация тестов в verify** — трижды исполненная практика (слепой проверяющий ломает код во временной копии и требует падения теста; пустой тест \= дефект) оформляется записью; touch-кандидат.  
2. **INFO/строка: контекст-диета** (кураторский отбор агентов/команд/скиллов ECC) и **дневной repo-дайджест heartbeat** — зафиксировать существование, по строке каждому.  
3. **Уроки OpenAI harness-engineering** — четыре строки в touch-очередь/знания (НЕ исполнять до freeze, только зафиксировать кандидатами): gardener-процесс (heartbeat-скан расхождений знаний↔поведения → fix-предложения); HARNESS-CHECKLIST \+2 пункта («софт-продукт обязан быть управляем агентом: запуск/прогон/чтение телеметрии — в DoD» и «непонимаемая агентом зависимость → вендорим подмножество/пишем своё»); quality grades продуктовых доменов; словарь «agent harness / harness engineering» \+ строка-валидация OpenAI-конвергенции — в README/питчи (уйдёт в ATOM-120). В NOT-DOING с обоснованием: «humans not required to review» и минимальные merge-гейты — их контекст, не наш (наш периметр-гейт \= продукт для нетехнических фаундеров и банков).

## Блок 2 — микроскопия пользовательского пути (в существующий ATOM-120, зачёт не трогает)

Из брифа Codex, принято: README-порядок «что я получу за 15 минут → потом философия»; distribution-README без деталей до установки; каждый вывод install.sh \= действие/статус/точный fix; финальный экран инсталлятора \= ровно 2–3 команды, без рассеивания; «qroky start работает там, где инсталлятор обещает». Это чек-лист качества README/кита, не новые правила.

## Блок 3 — после зачётного прогона CEO (INFO-035) и волны №1, до волны №2

4. **Freeze v0.2 — исполнить учреждённое** (шаги 1–7 RELEASE) с классификацией Codex: каждый пункт корпуса → **keep / merge / archive / mechanize** («mechanize» — новая корзина: правило, переводимое в скрипт, переводится). Deletion test с прецедентом, консистенси-скан, регресс смены модели, теги адресата.  
5. **Дерево scripts/verify.sh** — механизация инвариантов одним прогоняемым скриптом (перед каждым релизом): нет секретов в tracked/logs/backup; bash \-n на все скрипты; lang/*.sh — одинаковый набор функций; README-обещания \= поведению install.sh (число вопросов, флаги \--uninstall/--check-update/--apply-update/--enable-*); status.yaml — базовая валидация схемы; битые ссылки README/TASKS; Telegram-токен не логируется; machine-wide пишет только обещанные файлы. Существующие DoD-проверки атомов кита — собрать, не переписывать.  
6. **Shell-tightening атом** (без rewrite): длинные функции; дублирование install.sh/dry-run.sh/telegram-runtime; изоляция самописного JSON/YAML-парсинга в маленькие функции с тестами; destructive paths печатаются до действия; network calls — timeout+retry limit+человекочитаемый отказ; **EOF/non-interactive никогда не выбирает молча небезопасный default** (проверить особо — кандидат в реальный баг). Verify — мутационным методом (п.1).  
7. **Release-readiness сводка** (micro): одна команда/файл «current release readiness» \+ статус clean-run-blocker'а; секция в heartbeat-дайджест.

## Блок 4 — SEPARATION (отдельное дерево, после блока 3, до волны №2)

Три дома: **qroky/framework \= чистый продукт** (конституция, runtime, скилл, кит, docs, knowledge — он универсален целиком, CHANGELOG; ноль run-артефактов, ноль наших атомов, ноль TASKS); **qroky/lab \= публичная фабрика** (вся наша стройка с историей: атомы, decisions пути, NARRATIVE'ы, TASKS — README продукта ссылается «вот полный журнал самопостроения»); **инстансы — приватные, как есть**. Механика: **dist-manifest** — явный whitelist путей дистрибутива; вендоринг инсталлера — sparse по manifest (сейчас уезжают все 576 файлов тега — вес, контекст-шум чужой истории в каждой сессии пользователя, путаница идентичности «чей это TASKS.md в моей системе»); freeze-проверка «дистрибутив \= только manifest-пути» — в verify.sh. Правка install.sh \= новый зачётный прогон по INFO-035 — потому строго после волны №1.

## Порядок исполнения (сводно)

Зачётный прогон CEO (v0.3.1, как есть) → блоки 1–2 → волна №1 (тёплая раздача) → блок 3 (freeze v0.2 \+ verify.sh \+ tightening \+ readiness) → блок 4 (SEPARATION) → волна №2. План с конвертами — CEO на го; конверты считай по калибровкам (INFO-019), соразмерность (INFO-018) — деревья маленькие, вееров не ждём.  
