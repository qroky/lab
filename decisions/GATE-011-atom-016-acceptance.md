---
id: GATE-011
date: 2026-07-06
kind: gate
status: answered
atom: ATOM-016
trigger: E3
answered_by: Ghenadie (human)
recorded_by: formulating agent (launch session)
---

# GATE-011 — Acceptance of ATOM-016 (SYNTHESIS-PROTOCOL)

## Question / Brief

Brief: `products/framework-v1/016-synthesis-protocol/GATE-BRIEF-GATE-011.md` (GB3 —
referenced, not duplicated). Requested: go / no-go / pivot on accepting
`framework/SYNTHESIS-PROTOCOL.md` as `reviewed` after a round-1 blind Verify
`accept` with zero findings, plus the human judgment on deferred criterion S5
(VP9): would the protocol, applied retroactively to business-strategy-v1, have
forced at least one same-question multi-lens examination with at least one named
conflict.

## Answer (verbatim)

Gate decision:

> Go — принять как reviewed (Recommended)

S5, first response (to the brief-form question):

> Я не понял вопроса и не понимаю, что ответить.

S5, after the formulating agent restated the criterion in plain language (would
the protocol, had it existed yesterday, have forced the two experts onto one
question and delivered their "startups vs banks" disagreement as an explicit
fork):

> Да — создал бы, S5 выполнен

English summary (HP3): The human accepted ATOM-016's product as `reviewed` (go).
On deferred criterion S5 he initially could not parse the criterion-form question;
after a plain-language restatement he judged S5 **met** — the protocol would have
created a real cross-lens collision on the strategy run, i.e. it changes outcomes
rather than decorating them.

## Consequences

- `framework/SYNTHESIS-PROTOCOL.md` maturity `reviewed` conferred (M3); the fifth
  constitutional document is binding on every atom formulated after this
  acceptance (PM1 discipline).
- E8, `-SYNTH` ids, and the `SYNTHESIS.md` reserved filename join ATOM-SPEC §5.7,
  NC1, and NC7 at those documents' next touch (SP5, SY1 — HP4/GB5 migration
  discipline). Added to the queued constitutional touch.
- ATOM-016 closes per L7/RB6; the perspective axis awaits `validated` via the
  first real lens fan (candidate: ATOM-007 or the pilot product).

## Fed back to

- `knowledge/precedent/verify-envelope-calibration.md` — 7th verify measurement
  (139,532 vs ~180k formula envelope — held) and the executor-side lesson: the
  ATOM-016 executor ran +16% over its ~2.9×-read envelope (150,888 real vs ~130k);
  normative-writing executors need the same ×3.5(+40k) sizing as verifiers, the
  EC1 2× floor is not a sufficient envelope for constitutional writing.
- Gate-brief practice: the S5 exchange reinforces the GATE-010 reservation already
  queued for the constitutional touch — briefs that ask the human to judge
  document-shaped criteria must carry a plain-language restatement, not only the
  criterion text.
- `TASKS.md` — ATOM-016 moved to closed; ordering question 2.2 updated.
