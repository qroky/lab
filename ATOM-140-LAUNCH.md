# ATOM-140 [Q?] — Проработка генеративной веб-витрины (вердикт, не строительство)

- **Тип:** [Q?] study — вердикт-документ; НИКАКОГО кода в продукт
- **G1:** INFO-045 (решение CEO: «один [Q?]-атом проработки разрешён до
  волны №1»)
- **Роль:** свежий M/L-исполнитель (design study)
- **Конверт:** ~85k (executor ~70k, verify slim ~15k — соразмерность
  INFO-018: study-атом). ⚠️ INFO-019 flagged; исчерпание → честный E4.

## Зачем (два потолка)

Ближний: CEO нужен обоснованный план v1 витрины (состав, вид, цена) для
«го» после волны №1. Дальний: третья голова снимает bash-стену для
нетехнических пользователей и открывает multi-user наблюдение —
первая ступень enterprise-этажа (INFO-045).

## Мандат (DoD)

1. **Вердикт-документ** `products/web-vitrina-v1/140-study/VERDICT-STUDY.md`:
   - Состав v1 в жёстких границах INFO-045: read-only, файл+браузер, без
     серверов/портов, оффлайн, рендер S/M из существующей шины
     (status.yaml, NARRATIVE.md, RESULT'ы, cost), ≤5% subtree_cost.
   - Механика: как `qroky dashboard` рендерит и держит свежим (heartbeat
     и/или по команде); где живёт шаблонизация; как форма UI выводится из
     типа проекта (F2-принцип: UI — производный продукт проекта).
   - Что НЕ входит в v1 (границы дословно) + NOT-DOING чат-клиента.
2. **Два эскиза** — статические HTML-моки (это артефакты study, НЕ продукт;
   лежат в workspace/): (а) сад-проект — карта участка/этапы/смета — данные
   бери из реальных артефактов сад-теста в repo (products/
   pilot-prerequisites-v1 и связанные), чего нет — мокай и помечай MOCK;
   (б) бижутерия (креатив: галерея/этапы/варианты) — полностью MOCK по
   типажу из INFO-045. Эскизы должны открываться локально одним файлом
   каждый, без сети.
3. **Экономика**: оценка стоимости построения v1 (в токенах, по
   калибровкам ролей из roles/pilot-toolsmith.md maturity) и стоимости
   ПОДДЕРЖАНИЯ (рендер на heartbeat: токены/день на живом проекте средней
   активности — посчитай от реальных объёмов шины rpf).
4. **Риски/открытые вопросы** — коротким списком (включая: где граница
   «тип проекта» определяется — intake или эволюция; что с двумя
   проектами на машине — по-проектная витрина или сводная).

## NOT-DOING

Код в distribution/runtime; правки install.sh/qroky.sh; сервера, порты,
JS-фреймворки с CDN (моки — self-contained); собственный чат-клиент
(NOT-DOING INFO-045 — в вердикте только зафиксировать).
