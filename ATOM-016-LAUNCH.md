# LAUNCH — ATOM-016 (SYNTHESIS-PROTOCOL: the perspective axis)

> Runtime instruction for this session. Execute **1 atom** in strict sequence, per the
> framework documents (`framework/ATOM-SPEC.md`, `framework/FEV-PROTOCOL.md`,
> `framework/REPO-STRUCTURE.md`, `framework/ORCHESTRATION.md` — read them first) and
> the binding (`runtime/claude/README.md`).
> Step 0 — materialize the INPUT specification below into its atom folder per LP3:
> `products/framework-v1/016-synthesis-protocol/INPUT.md` verbatim; `STATUS.md` opened
> at `formulated`; `products/framework-v1/status.yaml` entry per SM3.
> Execute; then formulate and instantiate the blind Verify atom per FEV-PROTOCOL §2
> (mode `independent + human`): fresh isolated session (VP4, SS6), tier L (MT3/MT4),
> envelope calibrated over the blind package per EC2 and the ×3.5+40k precedent
> (`knowledge/precedent/verify-envelope-calibration.md`; expect ~150k for a ~95 KB
> package). After Verify accepts: gate brief in Russian per HP2, authored by this
> orchestrating session — never by the executor (GB4) — human sign-off recorded as a
> GATE decision record (VP18). Close per L7/RB6, push included. Update `status.yaml`
> at every transition.
> Spawn justification (formulator): constitutional gap — the framework recurses by
> product size only; the perspective axis (multi-lens examination of one question,
> reconciling synthesis, semantic fold-back) is absent as a primitive; one atom, one
> new framework document (LP6).

---
---
atom: ATOM-016
product: Synthesis & Perspective Protocol (framework/SYNTHESIS-PROTOCOL.md)
parent: Recursive Product Framework v1
formulated_by: Human (Ghenadie — design decisions INFO-004) + external advisor session, 2026-07-06
verification: independent + human
maturity_target: reviewed
instantiation: session
model_tier: L — constitutional normative writing binding every future atom (MT3; doubt impossible per MT6)
budget: ~130k tokens (executor; reads ~45k, EC1 floor 2×, plus normative-writing margin). Verify envelope calibrated separately by the orchestrator per EC2 at Verify formulation. Hard stop per O6.2.
recursion_allowance: 1
---

# ATOM-016 — Input Specification

## 1. Product Identity
| Field | Value |
| :---- | :---- |
| Product | `framework/SYNTHESIS-PROTOCOL.md` (LA4 — deliverable outside the atom folder, path named here) |
| Atom ID | ATOM-016 |
| Parent product | Recursive Product Framework v1 |
| Executor role | framework-architect (exists — F2 satisfied) |
| Input product(s) | `framework/ATOM-SPEC.md`; `framework/FEV-PROTOCOL.md`; `framework/REPO-STRUCTURE.md`; `framework/ORCHESTRATION.md`; `decisions/INFO-004-perspective-protocol-design-answers.md`; prior art (read-only, external to this repository): `/Users/ghcernei/Work/_BUSOS/council/PROTOCOL.md` and `/Users/ghcernei/Work/_BUSOS/council/templates/`; `knowledge/precedent/` (both files) |
| Consumer(s) | Every future formulating agent (the map and fan duties enter Formulate); synthesis atoms (their operating manual, as FEV-PROTOCOL is for Verify atoms); Verify atoms (new criteria become checkable); the human at gates (lens conflicts arrive as named trade-offs, not stacks of documents) |
| Maturity target | reviewed |
| Gates | none placed — trigger gates only; human sign-off per verification mode (brief in Russian per HP2) |
| Instantiation | session |
| Verification mode | independent + human — F3 row 1: high criticality (constitutional document feeding every downstream atom) |
| Budget envelope | ~130k tokens |
| Recursion-depth allowance | 1 |

## 2. Job To Be Done

Close the framework's missing horizontal axis. Today the framework decomposes by
**product size** only (A4/K4); the atom formula binds exactly one role per atom
(§1.1); nothing obliges anyone to examine one question through several role-lenses;
there is no protocol for integrating divergent views — FEV-PROTOCOL is a full
protocol for *judging correctness* of one product, and no protocol exists for
*reconciling perspectives* into one view; and fold-back lifts only accounting —
status (SS3), cost (O4.2), maturity (M3) — never meaning.

Deliver the constitutional document that fixes this: after it, a formulating agent
holding the five framework documents is **obliged** to (1) derive a perspective map
at Formulate time — which lenses must examine this product, derived from the whole,
not templated; (2) open per-lens sibling atoms **on the same question** where the
fan triggers; (3) run a synthesis act that names contradictions between lenses the
way Verify names findings, reconciles facts, and gates values to the human; and
(4) fold the integrated view back into the parent's own product on **both** axes —
perspective fans and ordinary mono-role decomposition alike.

Empirical bar *(informative)*: applied retroactively to business-strategy-v1, this
protocol would have put at least two lenses on the same question at least once and
surfaced at least one named cross-lens conflict — something that run never produced.

## 3. Context — Decisions Already Made (INFO-004 — formalize, do not reopen)

- D1 — **Threshold.** The perspective **map** is mandatory broadly: every Formulate
  act includes it. Opt-out is a single recorded line — "single lens — narrow
  question" — logged as a deviation in the F3 discipline (justification logged, never
  silent). The execution **fan** (per-lens sibling atoms) is selective: mandatory
  when any of — a decomposition plan exists (OD1), maturity target ≥ `reviewed`, or
  the product crosses the perimeter.
- D2 — **Synthesis authority.** Conflicts of **fact** are reconciled by the synthesis
  act itself, with the reconciliation logged. Conflicts of **value** always gate to
  the human. Classification is by the *nature* of the conflict, not its magnitude.
- D3 — **Fan budget.** Lens atoms are siblings inside the parent's envelope (O6.1).
  Weights are assigned by the perspective map — default equal, with a minimum
  envelope per lens so no lens is starved. Lens atoms MAY run at tier S/M; the
  synthesis act always runs at tier L (extends the MT3 principle: quality is bought
  at Formulate, Verify — and now Synthesis).
- D4 — **Lens depth.** Sub-lenses are permitted, charged in the same depth currency
  against recursion_allowance (F6, default total depth 3). The default fan is flat;
  opening lens depth requires a logged justification (the O5.1 discipline).
- D5 — **Semantic fold-back, both axes.** The parent of any decomposition —
  perspective fan OR mono-role product decomposition — MUST integrate its children's
  products into its own view: an integration act with contradictions named, not mere
  collection or aggregation. This closes the accounting-only fold-back gap.
- D6 — **Prior art.** The human's council mechanism (multi-role clash + chaired
  synthesis; input paths in §1) is source material: extract the mechanics of
  clashing views without smoothing conflicts, and of a chaired decision. Neutrality
  per RC3: no tool or product names appear in the framework text.
- D7 — **Cost visibility.** Cost blocks of fan parents report per-lens subtree
  lines, extending the O4.2 aggregation, so the cost of the fan is visible per lens.
  *Known gap:* INFO-004 records that the detail of this decision was referenced but
  not fully transmitted; the minimal interpretation above is the accepted decision.
  If it under-determines a rule, raise E1 — do not invent the detail.

## 4. Deliverable

`framework/SYNTHESIS-PROTOCOL.md` — one new constitutional document, sibling and
structural twin of FEV-PROTOCOL. It tightens the existing four documents per R0.3
and never relaxes them; it does not edit them (forward references at their next
touch follow the HP4/GB5 migration discipline).

## 5. Definition of Done

**Hard criteria (machine-checkable):**
- H1. `framework/SYNTHESIS-PROTOCOL.md` exists and opens with the constitutional
  header table (Product / Parent product / Produced by / Maturity / Date). Check:
  file present; header fields grep-able.
- H2. The document declares its rule prefixes in its §0, and none collides with the
  existing set: R, A, F, C, U, RS, CL, K, V, M, L, E, O, T (ATOM-SPEC); P0, FP, VP,
  EP, DR, TP (FEV-PROTOCOL); LA, NC, KL, HP, RB, SM, BC (REPO-STRUCTURE); OD, MT,
  EC, GB, LP, SS, RC (ORCHESTRATION). Check: grep declared prefixes against this
  list; id-space disambiguation stated where letters could be confused (the §0
  discipline of ORCHESTRATION).
- H3. A coverage table maps each accepted decision D1–D7 (§3 above) to the rule
  id(s) implementing it (the RC4 discipline). Check: table present; every D row
  non-empty.
- H4. The document contains a fenced template for the perspective map and a fenced
  template for the synthesis product; every fenced block in the document is one of
  its declared templates (T2 discipline). Check: fence count equals declared
  template count; both named templates present.
- H5. The document's §0 states the R0.3 precedence: it tightens the earlier four
  documents and never relaxes them; an apparent relaxation is defective and the
  earlier document prevails. Check: grep.
- H6. Document length ≤ 500 lines (constitutional size discipline; the four existing
  documents run 250–420). Check: `wc -l`.

**Soft criteria (judgment — judge named per criterion):**
- S1. Tightens-never-relaxes actually holds: no rule in the document relaxes any
  obligation of the existing four documents — judge: Verify agent.
- S2. Scale invariance (A5): the map, fan, synthesis, and fold-back rules work
  unchanged for a paragraph-scale product and a company-strategy-scale product —
  judge: Verify agent.
- S3. The fact/value conflict classification (D2) is operational, not aspirational
  (the FP7 bar): two independent judges applying the document to the same conflict
  would classify it the same way — judge: Verify agent.
- S4. The threshold rules (D1) do not recreate bureaucracy at small scale: the cost
  of producing the map is negligible against the cost of the fan it may trigger —
  judge: Verify agent.
- S5. Applied retroactively to business-strategy-v1, the protocol would have forced
  at least one same-question multi-lens examination with at least one named conflict
  — judge: human (at sign-off).

**Maturity target:** reviewed. Do not gold-plate.

## 6. Method Hints (non-binding)

- Mirror FEV-PROTOCOL's skeleton: Formulate-side duties (the map), the act's
  protocol (synthesis as the twin of §2 Verify), escalation, templates. Candidate
  prefixes PM (perspective map), SY (synthesis), FB (fold-back) are free.
- The value-conflict human gate (D2) is naturally a new escalation trigger; define
  it in this document with a forward reference to ATOM-SPEC's E-table at its next
  touch (the HP4/GB5 migration discipline) rather than editing ATOM-SPEC.
- The perspective map likely lives as a section of the decomposition plan (§8.2 of
  ORCHESTRATION) plus a line in `INPUT.md` — prefer extending existing templates
  over inventing new files; LA2 keeps the layout closed.
- A synthesis act is itself an atom (A1 — no new unit of work): give it identity,
  folder, and product conventions the way VP6 does for Verify atoms.
- The rebrand swap (TASKS §2.1) is pending: refer to "the framework", keep any
  brand mention sed-replaceable.
- Semantic fold-back (D5) closes the audit's vertical finding too: state it so it
  binds ordinary A4/K4 decomposition, not only perspective fans.

## 7. Escalation

Stop and escalate (one consolidated question set) on triggers E1–E7 of ATOM-SPEC
§5.7. Known gap flagged at Formulate time: D7's detail is incomplete in INFO-004 —
if it under-determines a rule, that is E1, not a judgment call. Gates placed for
this atom: none — trigger gates only; human sign-off per the verification mode,
brief in Russian per HP2.

---
*End of launch file. Formulated from the 2026-07-06 framework audit (perspective
axis diagnosis) and the human's design answers recorded verbatim in INFO-004.*
